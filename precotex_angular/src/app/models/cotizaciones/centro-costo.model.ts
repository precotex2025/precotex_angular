/** Forma normalizada para el combo de centro de costo (ver getListaCentroCosto,
 *  que mapea CentroCostoRawItem → esta forma). */
export interface CentroCosto {
  codigo: number;
  nombre: string;
}
